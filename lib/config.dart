/// Para el endpoint, la info de appwrite y las funciones y url que terminan en /executions
/// 


class AppConfig {
  static const endpoint = '';   
  static const projectId = '';
  static const attendanceFunctionBase =
      '';
}
